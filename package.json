{
  "name": "lane-configuration-screen",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "src/server",
    "src/client"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:server": "npm --workspace src/server run test",
    "test:client": "npm --workspace src/client run test"
  }
}
